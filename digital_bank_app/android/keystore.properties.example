# Copy this file to android/keystore.properties and fill values to sign release APKs
# Paths are relative to the android/ directory, for example: ../keystores/myreleasekey.jks
storeFile=../keystore.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
